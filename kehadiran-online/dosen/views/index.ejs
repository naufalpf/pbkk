<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="font-awesome.min.css">
    <link rel="stylesheet" href="styles.css">
    <title></title>
  </head>
  <body>
    <div class="top">
      <h1>Absensi Kehadiran Mahasiswa</h1>
      <p>Klik simbol "Jam" jika On-time</p>
      <p>Klik Simbol "Orang berlari" jika Telat</p>
    </div>

    <form action="/addstudent" method="POST">
      <span>Nama</span>
      <input type="text" name="name" placeholder="Nama Mahasiswa">
      <div>
      </div>
      <span>Kelas</span>
      <input type="text" name="lastname" placeholder="Kelas">
      <button type="submit" name="button">Cek</button>
    </form>

    <ul class="studentList">
      <!-- Top row -->
      <li class="students">
        <span>Nama</span>
        <span>Kelas</span>
        <span>Tanggal</span>
        <span>On time</span>
        <span>Late</span>
        <span>Delete</span>
        <span>Kehadiran</span>
      </li>
      <!-- messages.length is the amount of objects in the collection  -->
    <% for(var i=0; i<students.length; i++) {%>
      <% let thisDate = new Date(students[i].date*1000)
      let dateString = thisDate.getUTCMonth()+"-"+thisDate.getUTCDate()+"-"+thisDate.getUTCFullYear()
      %>

      <li class="students">
        <span><%= students[i].name %></span>
        <span><%= students[i].lastname %></span>
        <span><%= dateString %></span>
        <span><img class="fa fa-thumbs-up" src="images/ontime.png"aria-hidden="true"></span>
        <span><img class="fa fa-thumbs-down" src="images/late.png" aria-hidden="true"></span>
        <span><i class="fa fa-trash" aria-hidden="true"></i></span>
        <span><%= students[i].att %></span>
      </li>
    <% } %>
    </ul>

    <script src="main.js"></script>
  </body>
</html>
